# 安全策略

cleancode 会运行本地 shell、PTY 进程、编码 Agent CLI 和会话级 MCP
服务器。我们会认真处理每一份安全报告。

## 支持范围

在首个稳定版本发布前，安全修复只应用于默认分支的最新版本。更早的提交和本地
开发快照不会作为独立版本线提供支持。

## 报告安全漏洞

请勿为疑似安全漏洞创建公开 Issue。

请使用 GitHub 的私有漏洞报告：

<https://github.com/chen-985211/cleancode/security/advisories/new>

如果条件允许，请提供：

- 受影响的操作系统和 cleancode 版本或提交。
- 受影响的功能及复现所需配置。
- 复现步骤或最小概念验证。
- 预期与实际观察到的安全边界。
- 潜在影响。
- 建议的缓解措施。

请勿提交真实凭据、私有源代码、个人数据或无关的终端历史。请使用最小的模拟值
代替这些内容。

## 重点安全边界

涉及以下范围的报告尤其有价值：

- MCP 鉴权、会话隔离、令牌泄露或审批绕过。
- 命令、参数、环境变量或 shell 注入。
- 工作区路径穿越或不安全的文件链接打开行为。
- 从当前工作区逃逸的符号链接。
- 跨项目、跨 worktree、跨 Agent 或过期会话的状态泄露。
- 错误的进程所有权、端口所有权或不安全的进程终止。
- 暴露数据或恢复到错误运行时身份的恢复文件。
- Electron IPC 发送方校验或 preload API 授权。
- 写入日志、审计记录、临时文件或持久化状态的敏感数据。

## 漏洞披露

公开披露前，请为调查和准备修复预留合理时间。维护者会通过私有安全公告协调
披露细节。
