# 终端会话生命周期

## 文档地位

本文是当前已实现 Run 终端 PTY、类型化 owner、权威终端模型、视图协议和前台任务技术生命周期的统一维护入口。终端依赖图、任务/服务调度与 `WorkflowRun` 由[终端依赖工作流](terminal-workflow.md)单独维护；Provider 对话和 Agent 活动语义由 [Agent 与会话生命周期](../agent/agent-session.md)维护。

全仓边界以[架构文档](../../engineering/architecture.md)为准；终端界面的稳定交互以 [UI 契约](../../product/ui-contract.md)为准。

## 能力状态与范围

Run 终端会话负责：

- 在指定工作区目录启动交互式 shell 或带启动命令的 PTY。
- 转发输出、写入键盘输入、发送 Ctrl+C、调整行列和终止进程。
- 记录精确运行作用域、进程 ID、状态、输入历史、退出码或启动失败原因。
- 查询运行中 PTY 的当前工作目录，用于工作区切换安全判断。
- 保持同一稳定画布对象槽位只有一个当前会话，并隔离旧 generation 的迟到事件。
- 以 `block | agent` 类型化 owner 区分普通终端积木与 Agent terminal；owner kind 进入规范画布对象身份、槽位和完整运行身份。
- 在 agent-owned terminal 的长期 shell 中启动可重复的 `ForegroundJob`，权威区分前台任务退出与 PTY 退出。
- 为每个可恢复运行维护进程内权威终端模型、单调输出序号和有界屏幕快照。
- 通过独立本地 Provider 承载普通终端 PTY 和权威模型；用户可以按当前会话选择应用退出后继续运行。
- 在应用重开时区分精确 live attach、只读历史恢复、自然结束和新会话，并校验完整运行身份与 Provider 证据。
- 在普通终端视图 attach、detach 和重建时协调输出投递与 terminal query 响应权。
- 统一当前与后续权威模型的滚动历史预算，并为安全打开终端链接提供精确运行上下文。
- 在 Project/BlockGraph 生命周期变更时异步硬清理匹配 PTY、恢复资料及其受管资源；应用退出只保留明确选择且可可靠 checkpoint 的普通会话。

它不拥有终端积木配置、工作区选择、终端依赖图、Agent Provider、对话绑定、MCP 或活动状态；这些事实分别属于 BlockGraph、Project 与 Agent。

## 聚合与状态

`TerminalSession` 是 Run 上下文的聚合根之一。状态机为：

```txt
idle -> running -> stopping -> exited
  \-> failed       \-----> exited
```

| 状态       | 含义                                         |
| ---------- | -------------------------------------------- |
| `idle`     | 聚合已创建，但尚未成功取得 PTY 进程          |
| `running`  | PTY 已启动，允许记录和转发输入               |
| `stopping` | 已发起异步终止，不再接受新的交互操作         |
| `exited`   | 进程自然退出或被终止；自然退出保存真实退出码 |
| `failed`   | PTY 启动失败，保存失败原因且进程 ID 保持为空 |

`recordInput` 只允许在 `running` 状态执行。Ctrl+C 只是向当前 PTY 写入 `\x03`，不会把会话伪装成已退出；关闭会话才终止进程并标记 `exited`。普通直接启动命令运行在保留会话的交互式 shell 启动包装层中，命令自然结束或被 Ctrl+C 中断后必须回到可输入的 shell；依赖工作流有限任务和受管端口服务仍使用随命令退出的 PTY，以保留退出码调度与端口清理语义。

会话还记录 `kind`、退出保留策略、恢复类型和不可变的 `terminalSourceTheme`。新 generation 从当前有效应用主题取得 `dark` 或 `light`；同一 generation 的普通启动、快速启动、受管服务、工作流、可见重挂载和恢复都必须沿用该值，应用主题切换不得改写它。`interactive` 与非工作流 `direct` 会话默认使用 `terminate-on-application-exit`，用户只可对当前运行的 block-owned 会话明确切换为 `keep-after-application-exit`。replacement 通常创建使用默认策略的新会话；唯一继承入口是用户从已保留的当前普通会话执行“启动命令”，此时新建的非工作流 `direct` 会话在自身 checkpoint 成功后继承保留策略。重开空会话、新建终端、工作流启动及其他 replacement 不继承。`workflow` 和 agent-owned terminal 永远不能启用退出保留；领域聚合必须同时拒绝新设置与携带非法策略的 live 恢复资料。恢复类型固定为 `fresh`、`warm`、`historical`、`ended`：只有 `warm` 可以同时声称进程仍运行；`historical` 必须没有进程 ID、不能写入或中断。

终端完整浅色/深色色板只在 `src/presentation/app-shell/styles/theme.css` 中维护一次；`node scripts/check-theme.mjs --write-terminal-palette` 从这些声明确定性生成 `TerminalPalette.generated.ts`，`pnpm check:theme` 拒绝缺失或陈旧产物。Run 的隐藏模型与 Presentation 的 xterm 都消费该生成模块。Windows Agent 前台启动和普通终端内被稳定 shim 捕获的 Codex 单次启动可以为了 Win32 默认色探测短暂选择 ConsoleColor 索引，但这项技术状态不得成为 xterm palette、普通 shell 生命周期或 Provider 用户主题；设置与恢复产生的 transport 字节必须在进入权威模型前被消费。`terminalSourceTheme` 只选择固定 generation 对应的 canonical palette，不把当前应用主题或 Provider CLI 的用户主题变成 Run 会话事实。

## 会话身份与隔离

每次启动都会创建新的 `sessionId`、`runId` 和单调递增的 `generation`。完整 `TerminalRunScope` 还包含项目 ID、稳定工作区 ID、项目/工作区目录、Git 分支启动元数据、兼容 block ID 和类型化 `owner`。`TerminalSessionService` 使用 Shared Kernel 规范身份 `projectId + workspaceId + objectKind + objectId` 识别当前槽位：

- 同一槽位启动新会话时，先异步终止并等待旧会话清理完成。
- 相同 owner ID 在不同项目、物理工作区或 owner kind 中拥有独立会话，可以同时运行。工作区目录和 Git 分支是启动、校验与显示元数据，不进入槽位 key；默认工作区 checkout 后继续指向同一 session、PTY、模型和视图身份。
- 进程自然退出后移除活动槽位映射，但保留最新 generation 的会话快照和有界终端模型。工作流面向用户的停止、超时和失败清理采用相同的历史保留结果：终止 PTY、把原会话标记为已结束并继续保留其最终模型；显式关闭、后续 replacement 或统一硬清理才撤销该资格。
- 输出、退出、视图、快照和服务端点事件必须同时匹配当前槽位的完整运行身份；旧运行的迟到回调不能覆盖新会话。
- 每次启动在取得 PTY 前通过 `RunRuntimeScopeValidationPort` 校验 Project 的权威项目、`workspaceId` 与目录，并受 `RunLifecycleService` 启动闸门保护；分支不参与稳定身份校验。
- 应用退出时先关闭新启动准入并等待在途启动；Agent 与工作流上下文只排空自己的在途操作，并在 Provider handoff 后释放本地引用，不逐会话发起 PTY stop。Agent 可以在 handoff 前按自身 contribution 的有界协议向当前前台任务写入正常退出输入并等待 launch 退出，以便对应 CLI 发布最终结构化事实；该动作不停止 shell/PTY，也不改变 Run 的所有权。Run application 只向独立 Terminal Provider 提交一次应用级 detach，Run/Terminal Provider 是该流程唯一的 PTY shutdown authority。Terminal Provider 终止全部 Agent、工作流和默认策略会话；只有可靠 checkpoint 的明确保留普通会话从应用 detach。应用断连或 Electron 停止等待不等于 Provider shutdown。
- 显式关闭、replacement、删除积木、归档/移除物理工作区、目录重绑定和移除项目始终是硬清理：终止精确 PTY、撤销恢复资格并删除 checkpoint，覆盖退出保留策略。删除完整流程或终端组合时，Run lifecycle 在同一个项目串行边界内为精确终端集合建立一个租约并清理全部匹配 owner，不能要求调用方逐终端持有嵌套租约。分支 checkout 不是硬清理条件。

Renderer 重新进入工作区或崩溃重建时，可以批量查询应用层已经接受的会话，并只按完整运行身份收敛状态。应用完整重开时，Run 先完成 Provider 对账再允许终端节点自动启动，避免新 shell 抢占 warm/historical identity。退出事件先于启动响应抵达时，表现层先用事件中的项目、工作区、终端和运行身份建立 `exited` 投影；同一运行迟到的 `running` 启动响应不得把它降级。已经保留但不再运行的 session 收到迟到 `write`、Ctrl+C 或 resize 时，应用层不再访问 PTY，而是幂等返回当前权威快照；这些交互动作指向未知 session 时仍返回 `TERMINAL_SESSION_NOT_FOUND`。终止动作表达“确保该 session 不再存在”，因此未知 session 视为已经完成并返回空结果，不能阻断随后启动新会话。

应用启动和 Provider 断连恢复使用应用级 `initializing -> ready | unavailable -> initializing` 可用性状态机。只有 Provider 会话对账和受管服务端点恢复都完成后才能进入 `ready`；`RunLifecycleService` 在其他阶段统一拒绝所有新启动并返回 `TERMINAL_RUNTIME_NOT_READY`。每次从非 ready 成功进入 ready 都递增 runtime epoch；renderer 先按该 epoch 查询并投影恢复会话，再让没有恢复身份的终端各自动启动至多一次。失败不能把某个终端永久锁在一次性的“已经请求过启动”标记中；用户显式重试或 Provider 断连后的成功恢复必须开启新 epoch。恢复查询失败时继续保持 pending，不得把“没有查到结果”当作“没有旧会话”并抢先创建 shell。

会话的 `workingDirectory` 来自 Project 工作区 DTO；Run 不自行读取或切换 Project 聚合，而是通过调用方拥有的校验端口验证该 DTO 仍然权威。

### Agent owner 与前台任务

`owner.kind = agent` 只开放基础终端和前台任务能力。它不能进入 BlockGraph 组合、依赖工作流、受管服务端口或普通终端批量动作；Agent 通过自己拥有的 `AgentTerminalRuntimePort` 和基础设施 adapter 调用 Run 应用层，Run 不反向依赖 Agent 类型。

Windows agent-owned terminal 在开放 `launchForegroundJob` 前必须先完成外层 PowerShell 的交互就绪握手。Run infrastructure 的单一 readiness owner 只接受精确 OSC marker 作为 `ready`，把 marker 前的 PTY exit 收敛为 `exited`，并用独立 safety deadline 防止启动永久挂起；deadline 只形成 `deadlineExceeded` 失败，不能证明成功。marker 可以跨多个 `node-pty.onData` chunk 到达，首个终局状态不可被迟到 output 或 exit 改写。失败诊断只包含耗时、接收字节数和 marker 匹配进度，不得包含 terminal 输出或环境变量；失败后沿 PTY 既有停止路径释放进程。

`launchForegroundJob` 为当前仍运行的 agent-owned terminal 创建单调 generation 和随机 `launchId`。macOS/Linux 把经过校验的 executable、argv 与 environment 写入 mode `0700` 的 POSIX 临时脚本，参数逐个 shell quote；Windows 通过 ConPTY 的长期 PowerShell 启动 ASCII-only 临时 `.ps1`，executable、argv 与 environment 值先按 UTF-8 Base64 编码，再在子 PowerShell 进程内恢复为独立参数和进程级环境变量。Windows 脚本必须在 started 控制帧之前重申 Console 输入、输出和 PowerShell native pipeline 的 UTF-8 encoding，再按固定 `terminalSourceTheme` 设置 ConsoleColor：浅色使用 `Black`/`White`，深色使用 `Gray`/`Black`；这样前一个前台任务即使改变 console encoding，也不能污染下一次 Agent launch。两条路径都校验环境名，并通过随机 Token 控制帧确认 started/exit；PowerShell `finally` 在 `Ctrl+C` 时仍报告本次 launch 退出，而不关闭外层 shell。从内部 probe 写入 shell 到 started 控制帧到达之间属于 `awaiting_started` 控制阶段，该阶段的 shell 回显、编码与颜色准备、临时脚本路径、状态变量和控制 Token 必须被消费，不能进入 transcript、snapshot 或用户屏幕；started 之后的 Provider 输出和 exit 之后的 shell 输出才进入权威模型。临时目录在退出、替换、terminal 终止或失败时幂等删除。

POSIX 前台任务可以提供可选 `fallbackPath`，由 Run 的 launch 脚本在子进程中追加到实时继承的 PATH 后；路径值按独立 shell word 引用，保留已有目录顺序，不回写外层 shell。显式 `environment.PATH`（包括空字符串）完全覆盖该合并结果，Windows 不使用此补充字段。该字段沿应用端口和 Provider RPC 透传，不承载 Agent Provider 身份或检测事实。

前台任务自然结束、被 `Ctrl+C` 中断，或响应拥有该 Agent Provider 协议的有界正常退出输入后，`ForegroundJob` 记录真实退出码，底层 shell 继续运行并接受输入。只有 shell/PTY 退出才使 `TerminalSession` 进入 `exited`。同一 terminal 可以顺序启动多个 job，旧 `launchId + generation` 的 started/exit 事件不能改变新 job；正常退出输入也只能作用于仍匹配的当前 generation，launch 一旦退出便停止发送剩余步骤。

## PTY 端口语义

Run 应用层只依赖 `TerminalProcessPort`：

- `start`：以工作目录、类型化 owner、shell、可选启动命令和行列启动 PTY，返回真实进程 ID。
- `launchForegroundJob`：在 macOS/Linux 的 POSIX shell 或 Windows 的 PowerShell/PowerShell Core 中，为仍运行的 agent-owned terminal 启动结构化 executable/argv/environment，返回 `launchId + generation`。Windows 普通 `cmd.exe` terminal 仍可使用，但不承载 Agent 前台任务。
- `write`：只向仍运行的 PTY 写入原始输入；已退出快照返回当前状态而不写入进程。
- `resize`：只同步仍运行的 PTY 行列，并返回当前会话快照供调用方对账。
- `pauseOutput` / `resumeOutput`：只用于模型背压和视图响应权交接，暂停读取 PTY 输出但不终止进程。
- `readWorkingDirectory`：只作为工作目录事件缺失、旧 Provider 或重新可见后的低频校验手段；在 macOS 通过 `lsof`、Linux 通过 `/proc/<pid>/cwd` 尽力读取，不支持或进程消失时返回 `null`。该辅助查询超时不得断开健康的 Provider 连接，也不得单独把 terminal runtime 标记为 unavailable。
- `stop` / `disposeAll`：异步终止一个或全部受管 PTY，并等待适配器确认退出。

基础设施默认在 macOS/Linux 使用用户 shell；Windows 调用方显式提供的 shell 仍然优先，未显式提供时采用 PowerShell 自动策略：优先解析可由 ConPTY 直接启动的真实 PowerShell 7 `pwsh.exe` 绝对路径，找不到时使用 `%SystemRoot%\System32\WindowsPowerShell\v1.0\powershell.exe`。解析不得把 Microsoft Store App Execution Alias stub 当作真实 executable；只有标准目录和绝对 PATH 均未发现真实文件时，才读取 alias 的 AppExecLink 目标，绝对目标直接规范化、相对目标按 alias 目录解析，并且只接受绝对、非空、不再属于 alias 的真实 `pwsh.exe`。readlink 失败或目标未通过安全校验时，才允许通过一次最长 2.5 秒、不加载 Profile 的非交互 discovery process 启动 alias 并读取 `$PSHOME`。默认 Windows shell 的 in-flight discovery 在 Provider 进程内共享，完成结果使用有界缓存且 rejection 不留存；解析为 inbox PowerShell 的结果使用较短有效期，以便从 Store 更新、PATH 变化或瞬时探测失败恢复。自动选择的 `pwsh.exe` 只有在同步 spawn 报告 `File not found` 或 `Cannot create process, error code: N` 时才单次回退 inbox PowerShell；inbox 成功后短期隔离该坏路径，避免后续 terminal 重复撞击，隔离到期后重新探测。显式 shell、ConPTY 初始化、readiness、Profile 输出和异步 exit 均不触发该回退，自动链不包含 `cmd.exe`。Windows 普通 `powershell.exe`/`pwsh.exe` terminal 使用 `-NoLogo -NoExit` 启动，不传 `-NoProfile`，Profile 仍按 PowerShell 原生规则加载；Profile 自己产生的输出沿既有 PTY 和权威模型链路呈现，不由基础设施或 renderer 过滤。Windows PTY 固定使用 node-pty 随包的 ConPTY DLL。

macOS/Linux 的交互式 zsh、bash 和 fish 使用 Provider 状态目录中的进程级 Shell Integration 发布标准 OSC 7 工作目录。私有启动片段必须保留并加载用户原有启动文件，不修改用户 dotfile；有限任务和其他非交互启动不注入该能力。安装失败、shell 不受支持或旧 shell 缺少所需能力时继续启动原 shell，并退回低频 `readWorkingDirectory` 校验，不能阻断 terminal readiness。

Windows PTY 从同步 `startProcess/connect` 到首个输出前都必须可立即停止，不得把 stop、destroy 或信号校验排队到 `ready_datapipe`。同一 terminal/agent 的重复关闭只认领一次 native shutdown；若 Windows 拒绝复制或终止进程 handle，native、agent 和 terminal 三层都会撤销一次性 guard，使后续关闭可以幂等重试。关闭会立即启动输出 Worker 的有界 drain deadline，即使进程从未产生输出也不能让 Worker 或 named-pipe server 常驻。原生 baton 在创建时保存匹配的 `ClosePseudoConsole` 入口；连接 pipe、属性列表初始化、进程创建或 exit watcher 安装失败时，native 层先回收未发布 baton、HPCON、pipe 和进程句柄，JS 层再逐项 best-effort 释放 Worker 与 socket 并保留原始启动错误。全局 handle mutex 只保护查找、连接中/已连接状态、认领和所有权转移，任何可能阻塞的 ConPTY/Win32 关闭调用都在锁外执行。

普通 Windows PowerShell 在 Profile 自然加载后通过 UTF-16LE Base64 的 `-EncodedCommand` bootstrap 设置 `[Console]::OutputEncoding`、`[Console]::InputEncoding` 和 `$OutputEncoding` 为 UTF-8；Agent 外层 shell readiness 命令也排在同一 bootstrap 之后。编码设置只在 `FullLanguage` 中 best-effort 执行；AppLocker/WDAC 强制的 Constrained Language 模式静默跳过受限的 .NET 构造器，不向普通终端注入可预期错误，也不阻断后续命令。短启动命令由 module-qualified `Microsoft.PowerShell.Utility\Invoke-Expression` 在 bootstrap 后重新解析并运行于当前会话作用域，既保留 `using`、script-level `param` 等必须位于命令首部的语法，也避免在受限模式下调用 `ScriptBlock.Create`；命令文本使用 PowerShell 单引号字面量规则转义，并继续由外层 `-EncodedCommand` 负责 Unicode 传输。外层编码结果超过 Windows 安全余量时直接保留原始 `-Command` 路径，不以编码优化制造启动回归。该过程不手动 source `$PROFILE`、不使用 `chcp`，也不修改 PTY 输出转发或 renderer 过滤规则；因此 Profile 自己在 bootstrap 之前产生的输出仍保持 PowerShell 原生行为。

Windows Provider 在首个已授权的会话对账结果构造完成后，仅通过一次 `setImmediate` 调度隐藏 ConPTY 预热，使协议响应先于可能同步阻塞的首个原生 spawn 写出。预热只解析真实 `pwsh.exe` 候选或系统 Windows PowerShell，不启动 Store Alias discovery，也不回退 `cmd.exe`；它以 `-NoLogo -NoProfile -NonInteractive -Command exit` 启动短命 helper，使用与真实终端相同的随包 ConPTY DLL，但不创建 session/model、不转发输出。这里的 `-NoProfile` 只属于不可见 helper，不改变普通终端保留 Profile 的契约。解析、spawn 或清理失败均只形成 Provider 诊断，不能阻断真实终端；Provider 信号关闭和自主退出都会取消尚未执行的预热或至多 kill 一次仍存活 helper。

有限任务和受管端口服务的启动命令通过 shell 参数执行，普通直接启动则使用明确的交互模式：POSIX 包装进程忽略发送给整个 PTY 前台进程组的 SIGINT，命令子进程恢复默认 SIGINT，命令结束后包装进程替换为用户 shell；Windows Agent 前台任务由子 PowerShell 执行，`Ctrl+C` 结束子任务后回到同一个外层 PowerShell。该模式不解析 shell 提示符、不延迟注入输入，并且只执行一次启动命令。环境变量覆盖在子进程边界注入，Windows 环境键按大小写不敏感规则处理；Provider 自身用于 Node 模式启动的 `ELECTRON_RUN_AS_NODE` 必须先从继承环境移除，只有调用方在本次命令环境中显式提供时才能重新注入。停止流程先解除 Run 自己施加的 PTY 输出背压，使退出事件和进程回收可以继续推进。POSIX Agent launch 清理随后从操作系统读取外层 PTY 的进程组和 TTY 当前前台进程组，不依赖 started 控制帧是否已经消费，也不假设 shell PID 等于进程组 ID；先给当前前台组一个有界正常收尾窗口，再终止外层 PTY 进程组，两者相同时只按前台收尾窗口处理，且都只在超时后升级强制信号。没有前台 Agent 的 POSIX 清理仍直接终止 PTY 进程组；Windows 关闭 ConPTY 并等待退出。该边界避免把 Agent launch 退出误判为 terminal 退出，也避免应用关闭时只回收外层 shell 而遗留前台任务；端口监听关闭仍由受管服务清理流程单独确认。xterm 的行列同步与视觉排障见[终端渲染排障指南](../../terminal/rendering.md)。

Run application 统一拥有终端能力环境。普通终端启动和 Agent foreground launch 都必须按会话固定源主题保留 `TERM=xterm-256color`、`COLORTERM=truecolor`、`TERM_PROGRAM=cleancode` 和对应明暗语义的 `COLORFGBG`；调用方或 Provider 以任意大小写提供的同名值都不能覆盖它们。其他 launch 环境继续透传，`NO_COLOR` 不由 Run 合成、清除或重命名，从而保留用户和上游明确选择的无色输出偏好。Provider contribution 不得把这些宿主能力变量变成自己的主题配置入口。

普通交互终端可以显式开启 Agent activity 宿主能力。Run 必须先创建完整 `TerminalRunScope`，再通过中性 `TerminalLaunchEnvironmentPreparationPort` 组合调用方已有的 launch preparation 与平台提供的 Agent 环境和可选私有 shell executable；工作流、有限任务和受管服务不隐式开启，也不会获得 shell override。平台向该 terminal generation 的进程环境前置应用私有 shim 目录，并注入私有 scope、HMAC token 和稳定 gateway manifest 路径；不得把 `ELECTRON_RUN_AS_NODE` 留在 Provider 环境、修改用户全局 CLI 配置或解析 PTY 输出。仅靠进程环境的初始 `PATH` 会被交互 shell rc 重排，因此没有启动命令的 POSIX bash/zsh 普通交互 shell 使用应用私有 shell launcher：bash 私有 rc、zsh 私有 `ZDOTDIR` 分阶段 source 用户原有启动文件，再在启动完成后重新前置 shim；已有用户 `ZDOTDIR` 必须保留并避免递归。带启动命令的会话、fish、sh 和 Windows 继续使用原 shell 与既有环境注入，不因可选能力改变启动语义。macOS/Linux shim 必须按 realpath 排除自身目录，避免 `/var` 与 `/private/var` 等路径别名递归；Windows 环境键按大小写不敏感规则合并 `Path`，同时提供 `.cmd` 与 PowerShell 入口。`.cmd` 启动器必须按 `cmd.exe` 的 argv 规则逐参数转义，不得使用可重新解析用户参数的 `shell: true`。Hook 上报只能携带规范身份和归一化信号，不能转发 prompt、回答、终端输出或完整环境。shim 必须异步启动 Provider、在 `Ctrl+C` 等终止信号下保持自身存活并先转发首个信号；Provider 超过短宽限期仍未退出时，POSIX 升级为 `SIGKILL`，Windows 通过无 shell 的系统命令强制结束对应进程树并以直接终止兜底。强制结束后仍未观察到 child close 时，wrapper 也必须在第二个有界期限后进入 `finally`，发布 invocation 退出并把交互控制权还给底层 shell。宿主初始化、Hook/plugin loopback 请求、本地初态和退出上报都必须有界并 fail-open。可选 activity 能力失败或缓慢不得阻塞 terminal 启动、Provider 进程或应用退出，首次资产 materialize 失败不得永久缓存 rejection，后续 terminal 允许重试初始化。

Run 仍是 terminal 生命周期 owner。终端自然退出、显式关闭、replacement、删除或恢复代际失效时，通过中性 lifecycle observer 发布精确 generation 已结束；每段输出通过当前身份检查并被权威模型接纳后，同一 observer 只发布完整 terminal scope 与单调 `sequence`，不发布输出正文。Agent 投影据此幂等释放全局活动状态，或重排尚未发布的完成静默窗；observer 失败必须 fail-open，不能改变输出接纳、下游投递或终端生命周期。应用重开时必须先完成 Run Provider 对账，再发布新的 loopback gateway manifest，使仍存活 terminal 中的既有 shim 可以使用持久 scope token 重新连接而不会把旧 generation 认作当前状态。

## 权威终端模型与视图协议

`TerminalSession` 继续拥有 PTY 业务生命周期；`TerminalModelPort` 只拥有同一运行在当前应用进程内的屏幕技术状态。PTY 输出通过 `TerminalSessionService` 的当前身份检查后，只进入权威模型一次，再携带该运行内单调递增的 `sequence` 交给应用消费者和当前视图。

每个模型使用与可见终端一致的行列、Unicode 11 宽度规则、会话源主题和当前滚动历史预算，维护 ANSI 屏幕、光标、颜色、alternate buffer、已支持模式、标题、工作目录和可读 transcript。滚动历史预算只接受 1000、5000 或 10000 行，默认 1000 行；设置变更同步到当前及后续模型和视图，并随 snapshot 明确传递。`TerminalSnapshot.sequence`、`terminalSourceTheme` 与 `RestoreMarker` 表示快照已经包含到哪个输出、应采用哪套 canonical palette；renderer 只接受同一 `viewId` 和完整运行身份的后续事件，并按序丢弃重复、检测缺口。

视图生命周期遵守：

1. `AttachView` 暂停 PTY 输出，等待模型完成已接受写入，生成 snapshot，并把 terminal query 响应权从隐藏模型交给该视图后恢复输出。
2. snapshot 生成和 IPC 返回期间的 live output 进入 renderer 的有界队列；renderer 先恢复 snapshot，再接续大于 snapshot sequence 的连续事件。
3. `DetachView` 只暂停输出并把响应权交还模型，不终止 PTY。旧 xterm 在主进程确认 detach 前保持可用，确认后立即销毁。
4. 缺失 sequence 或超过 1 MiB renderer 恢复队列时重新 attach；模型待解析输出达到 1 MiB 时暂停 PTY，降到 256 KiB 后恢复。
5. `ReplaceSession`、显式关闭和统一硬清理同时释放 PTY、模型、视图租约、缓冲与恢复文件；自然退出、工作流历史保留停止和 Provider 故障只保留有界最终模型，不伪造 live 状态。
6. `AttachView` 请求完整匹配当前运行身份、但对应 `TerminalSession` 仍处于 `idle` 启动窗口时返回 `TERMINAL_RUNTIME_NOT_READY`；renderer 可以对同一身份执行有界且可取消的重试，IPC 不为该预期生命周期收敛写 warning。尚未成功建立的 view 在该窗口 detach 时视为已经释放。
7. `AttachView` 返回 `RUN_SCOPE_STALE` 表示请求携带的完整运行身份已经终局失效；renderer 必须立即结束本次 attach，不得用同一身份定时重试。只有 React 接收到新的 `viewIdentity` 或 generation 后才允许发起下一次 attach。该结果仍通过结构化 IPC 错误返回调用方，但属于预期的作用域收敛，不记录 warning。

Provider Client 的全局普通终端输出投影只接收 block-owned terminal；agent-owned terminal 输出只进入其精确绑定的 view 和进程回调。表现层对找不到匹配运行身份的普通终端输出必须保持状态引用不变，不能把 Agent 全屏重绘暂存为普通终端启动输出，也不能触发画布节点重投影。

权威 headless terminal model 同时解析标准 OSC 0/2 terminal title 和 OSC 7 工作目录，并通过可选的结构化 metadata 回调交给调用方；这些 metadata 不是可见屏幕文本，也不赋予 Run 解释 Provider session 的职责。Terminal Provider 协议 v8 引入独立 `terminal-title` 事件，v10 引入 `terminal-working-directory` 事件；当前应用兼容已认证的 v8 至 v10 Provider。工作目录事件携带完整 `TerminalRunScope` 和该 session 内单调 revision，Client 只接受当前 generation 的更新并丢弃重复、倒序和旧 generation 事件；连接或查询期间到达的新事件优先于较旧的查询结果。既有 `terminal-output` 帧只由有界增量解析器提取标准 OSC metadata 并去重，不得匹配普通输出内容、记录 title，或把 metadata 误当作 Run 会话恢复资格；具体 Provider 只能在自己的 contribution 内校验其正式 title 格式。

renderer 以 `terminal-working-directory` 事件即时更新终端所属工作区。仅在窗口可见且某个运行中 session 连续 45 秒没有新事件时，才每 15 秒执行一次批量兜底查询；窗口隐藏时暂停查询，重新可见时补一次校验。事件订阅、兜底查询和工作区投影的失败都保留上一次权威模型缓存，不得禁用终端输入、中断、重启、固定或重连动作。

应用正常退出时，Electron main 必须依次关闭 Run 启动准入、释放当前视图租约、让 Agent 与工作流进入 prepare、把全部 PTY 一次性交给 Terminal Provider 处理，再让两个上下文 complete 并清除本地引用。Agent prepare 中可先请求当前 Agent Provider 前台 launch 正常结束，并在身份 reporter 仍存活时排空最终持久化；超时只结束这次 launch 级等待，之后仍进入统一 PTY handoff。整个 Electron runtime 清理使用默认 5 秒单调等待预算；预算只限制 Electron 为清理阻塞退出的时间，不取消已经提交的 Terminal Provider release，也不证明全部 PTY 已物理退出。Terminal Provider 必须在独立进程中继续安全清理并保留尚未完成会话的 owner 与诊断证据。正常完成的清理阶段保持静默，只有超时或失败才形成结构化诊断。Run 进入 `shutting-down` 后，新的或在途的终端工作目录同步查询收敛为空结果，不再访问已关闭的 Terminal Provider 或形成不可用告警；正常运行期的工作目录查询失败保留模型缓存，真实 Provider 断连仍由连接生命周期独立发布 runtime unavailable。renderer 销毁监听只负责异常退出兜底；同一个已认证 sender 只持有一个 `destroyed` 监听器并向其全部精确视图登记扇出，最后一个视图 detach 后必须移除该监听器。显式 detach、renderer 销毁和应用退出并发时，同一精确视图租约最多执行一次有效释放。已释放、已退休或未知视图的迟到 detach 是幂等清理，不得访问新 generation 的模型或记录生命周期噪声；attach、链接和其他业务动作仍严格校验完整运行身份。

因此，普通终端与 Agent terminal 的 renderer xterm 都是可丢弃投影，不是输出历史、屏幕状态或恢复资格的事实来源。隐藏终端不接收逐字节输出；macOS/Linux 的 terminal query 在任意时刻只能有一个响应者。OSC 10/11 默认前景色和背景色查询始终由权威 headless model 使用固定源主题回答，使协议响应不依赖可延迟的 renderer workload；可见 renderer xterm 必须消费同一查询但不再次响应。其他 terminal query 在视图接管期间继续由模型消费但不响应，并由当前 renderer xterm 唯一响应。

Windows 系统 ConPTY 会在 `node-pty.onData` 之前消费部分原生 TUI 通过 Console handle 发出的 OSC 查询和鼠标模式，只输出 screen buffer 差异；这会同时破坏默认颜色查询和可见 xterm 的鼠标输入。Run 因此固定启用 node-pty 随包的 ConPTY DLL，使完整 VT 序列进入既有模型/视图交接链路；OSC 10/11 由权威 headless model 唯一响应，其他查询由隐藏模型或当前 renderer xterm 中的一个响应。验证所用的 Codex 0.148 Windows 默认色路径不发送 OSC 10/11，而是直接用 `GetConsoleScreenBufferInfoEx` 读取当前 screen-buffer attributes 与 color table；`COLORFGBG` 和 xterm default palette 不能替代这项 Win32 事实。

Windows Agent foreground transport 在 Provider `started` 之前按固定 generation 的源主题把 ConsoleColor 设为浅色 `Black`/`White` 或深色 `Gray`/`Black`，其准备输出继续由 foreground control 消费。普通 Windows terminal 不得在 PowerShell bootstrap 中全局设置 ConsoleColor；只有 Agent launch spec 明确声明该 Win32 probe 的 Codex command shim 可以请求一次 provider-neutral private output control。Run 沿 `TerminalLaunchEnvironmentPreparationPort` 把固定 `terminalSourceTheme` 交给 Agent launch decorator；Agent 生成随机 token 并把 theme/token 作为 descriptor 返回。NodePty adapter 先清除公开或继承环境中同名的保留键，再只在自己支持 `osc-633-span-v1` 时原子注入 descriptor 私有环境；无效或缺失 descriptor 不能借宿主同名变量误启用 setter。shim 用匹配 token 的 `begin/end` 控制帧包住单次设置，Provider 调用期间移除 token 与主题环境；Provider 返回后先向模型保留一个可见 `SGR 0`，让 xterm 回到自身 default rendition，再用第二个私有 span 在 `finally` 恢复 Win32 ConsoleColor。Run 在 `node-pty.onData` 之后、权威模型、持久化和 view 广播之前消费精确控制帧及其括住的 ConPTY SGR，Provider 自身输出与尾部 reset 保持原样。解析必须跨 chunk、允许重复调用并在 4096 字符后有界 fail-open；错误 token 和其他 OSC 原样通过。旧 Terminal Provider 不理解 descriptor 时会连同私有环境一起忽略，宁可保留原有 Codex 降级，也不得执行没有输出 gate 的 setter。该协议不解析 Provider 正文或 activity，不改变 Claude、Gemini、OpenCode、普通 PowerShell 和直接 Agent 的所有权。

## 输入与安全打开边界

普通键盘输入、Agent 输入、IME 提交和粘贴最终都通过 `TerminalSessionService.write` 写入精确且仍运行的 session。没有文本选区时，xterm 产生的 `Ctrl+C` 原始 `\x03` 沿同一输入通道进入当前前台程序；有选区时表现层只复制选区。Presentation 可以在一个 16 ms 渲染帧内合并同一 session 的连续原始输入，以避免鼠标报告或快速键入把每个片段放大为独立 IPC/RPC；合并不得改变字节顺序，视图释放或运行身份改变时必须清除旧 session 的待发送输入。Presentation 还可以负责 composition 状态、剪贴板来源、用户确认、UTF-8 分片和 bracketed-paste 协议，但不得因此获得 PTY 生命周期、输入历史或当前运行身份的所有权。分片写入按调用顺序串行，取消或失败必须在已经打开 bracketed-paste 时尽力发送结束标记。

打开终端链接由 `OpenTerminalLinkUseCase` 重新建立授权边界：

1. `TerminalSessionService` 使用完整运行身份取得当前 workspace 与权威模型记录的最后工作目录；运行中的 PTY 工作目录探测成功时同时更新模型。
2. HTTP/HTTPS 目标交给平台外部打开端口；其他 URI scheme 一律拒绝。
3. 本地路径可以携带行、列提示，但必须先解析相对工作目录，再通过文件系统端口分别取得目标与 workspace 的真实路径。
4. 目标不存在、不是文件、越出 workspace、作用域已经失效或通过符号链接逃逸时失败关闭；平台打开能力只消费已经授权的绝对路径，不参与业务判断。

链接点击、搜索浮层、剪贴板确认和渲染器降级属于表现层交互，不写入 `TerminalSession` 或 BlockGraph。

## 启动命令与受管服务

空终端由 `TerminalSessionService` 直接建立，不执行 BlockGraph 启动命令。用户执行启动命令时，`LaunchTerminalCommandUseCase` 通过 `TerminalLaunchPlanPort` 读取不可变计划：普通任务和未声明端口的输出就绪服务进入常规会话；声明端口意图的服务进入共享 `ManagedServiceLauncher`，由其分配端口、注入命令、验证监听者并发布实际端点。

受管服务在端口分配或命令注入等 PTY 启动准备阶段失败时，本次 session 必须保留为 `failed` 权威快照并释放当前槽位，不能在已经发布精确冲突 identity 后删除 session。这样工作区切换后的状态对账和下一次“启动命令”终止旧运行都不会指向悬空 identity；下一次启动仍使用递增 generation。

直接启动、终端组合逐成员启动和依赖工作流中的服务节点必须复用同一个受管启动器。完整端口策略、租约和所有权语义见[本地服务端口治理](service-port-management.md)。

## 状态与持久化

`TerminalSession` 业务状态继续由 Run 聚合解释；BlockGraph 只持久化终端定义和启动意图，不保存运行实例。独立 Provider 在 Electron renderer/main 之外拥有普通终端 PTY、headless 模型和 live session identity，并通过带协议版本、随机认证材料、实例 ID、进程信息和单 controller 约束的本机帧协议与应用协作。PID 只是一项证据，不能在未验证 metadata、认证、协议、Provider instance 和完整 `TerminalRunScope` 时用于 attach、替换或终止。

Provider 启动协调使用短期、版本化且带唯一 owner 的本机租约。同一应用内的并发调用共享一次连接任务；空白或损坏租约先经过有界初始化宽限期，死亡 owner 和超期租约可以回收，旧 owner 迟到释放不得删除后继租约。应用退出先关闭新的连接准入并等待在途连接释放租约，再向已认证 Provider detach；断连期间只更新本地滚动历史设置，下一次任意连接在发送业务请求前统一同步最新值，不记录预期的断连设置警告。

当前 Provider 协议为 v10，并兼容已认证的 v8 至 v10 Provider。v9 在稳定 `workspaceId`、画布 owner 身份、`terminalSourceTheme`、结构化 `launchForegroundJob`、started/exited 定向事件与两阶段应用 detach 的既有边界上，为请求增加 controller lease 身份和有界 deadline；v10 继续增加携带完整运行身份和单调 revision 的 `terminal-working-directory` 事件。v9 与 v10 的受控请求必须携带当前 lease，v8 兼容请求可以不携带该字段。发现仍存活的 v7 或更旧 Provider 时以 `TERMINAL_PROVIDER_PROTOCOL_UNSUPPORTED` 失败关闭，不复用身份或请求隔离语义不一致的旧进程，也不为升级杀死其中仍被用户保留的普通终端。旧 Provider 自然结束后，后续应用启动按陈旧 metadata 流程创建当前版本 Provider。Provider 的并发关闭调用必须共享同一个完成 Promise，不能让后到的信号处理误判清理已经完成并提前退出进程。

`startProcess` 的 `privateOutputControl` 是可选、additive、provider-neutral launch decoration，不改变 session 身份、恢复 schema、协议认证或最低兼容版本。私有环境只存在该 descriptor 内，不能提前并入公开 `environment`；理解 `osc-633-span-v1` 的 NodePty adapter 才能原子注入并建立匹配 gate。兼容旧 Provider 时未知字段会被透传或忽略，但旧 adapter 不会注入其中环境，因此不会执行缺少 gate 的 Console setter；新客户端不得据此把该字段提升为必需协议版本。

`launchForegroundJob.fallbackPath` 同样为可选附加字段，不改变协议版本或恢复身份。旧 Provider 忽略该字段时仍继承存活 shell 的 PATH；新 terminal 的显式初始 PATH 仍生效，但既有 terminal 的刷新路径补充需要支持该字段的 Provider。不得为获得此能力强制终止用户保留的普通终端。

`health` 永远只读，应用必须在同一条已认证连接上显式 `claimController`，Provider 只允许 `unclaimed -> active -> releasing -> unclaimed` 的串行迁移。活动 controller 或释放中的 Provider 返回可重试的 `TERMINAL_PROVIDER_CONTROLLER_BUSY`；客户端在同一连接上有界等待，不能通过重复 health 或新建连接隐式抢占。正常 detach 和意外断连复用同一释放流程。`TerminalProviderShutdownCoordinator` 是 release 完成与各会话 checkpoint、stop、retire 有界预算的唯一协调 owner；controller lifecycle 必须等待该协调结果，不能再用独立外层截止时间越过仍在进行的持久化或补偿屏障。Provider 按 release 开始时捕获的精确 session identity，以默认并发度 8 处理互不依赖的会话：Agent、工作流和默认策略会话必须先确认 PTY stop，再 retire 会话与恢复资料；明确保留的普通会话必须先完成 checkpoint。保留会话 checkpoint 失败时立即转为终止候选，不能继续承诺跨应用恢复。stop、retire 或补偿 checkpoint 失败不得伪造成功或丢弃 owner/evidence；只要仍有存活的待终止会话，controller 就保持 `releasing` 并拒绝后继 claim，Provider 继续清理或保留可诊断状态。只有 release 后不存在不安全的 live 会话才允许后继 controller；不存在任何 live 会话时 Provider 才可以退出。应用进程另使用 Electron 单实例锁阻止正常情况下的重复主进程；协议控制权仍是保护现有 PTY 的最终边界。新启动的 Provider 必须写入当前 metadata 和协议版本。

每次 Provider spawn 使用独立的 `instanceId + heartbeatId` generation。子进程必须用不可变启动参数核对最终 metadata 的 generation 与真实 PID，之后才能创建 heartbeat 并绑定 generation 专属 endpoint；首次 heartbeat 在启动服务前发布，服务启动不能占用 heartbeat pulse 的串行队列。heartbeat 只更新既有文件的时间戳；回收方必须依据读到的文件身份、内容和时间戳进行条件撤销，若期间发生 pulse 则取消撤销，已经撤销的文件绝不由旧 owner 重建。瞬时 I/O 错误在 heartbeat 仍未跨过 stale 边界时只形成诊断，明确失去所有权或超过 stale 边界才关闭 Provider。客户端仍先以 token、instance 和协议完成真实认证，只有认证与 endpoint 均失败时才消费 heartbeat 存活分类；新 metadata 的 stale/missing heartbeat 可以在 PID 被无关进程复用时收敛为 dead，malformed、身份不匹配、不可读或未来时间一律按 unknown 失败关闭。metadata 写入、删除和终止迟到子进程都必须在 mutation guard 内按 `instanceId + heartbeatId` 再确认，删除前先条件撤销同代 heartbeat，迟到 owner 不得覆盖或删除后继 generation；没有 heartbeat 的旧 metadata 继续保留基于 PID 的兼容策略。

Windows 打包版的新 Provider 不直接从可被安装更新替换的应用目录运行。Platform 在启动锁内懒物化按 profile 与内容键区分的 runtime image，把改名 Electron-as-Node host、必需 ICU/V8 数据、物理 `app.asar` 与经过架构过滤的完整 unpacked `node-pty`/ConPTY 闭包保持原相对布局复制到本机 LocalAppData，再把实际镜像 key 写入同一份 Provider metadata。物理 ASAR 由 Electron `original-fs` 处理；冷路径内容键覆盖完整 host、archive、Electron 数据和 `node-pty` 闭包。完整发布写入 schema v2 marker；后续主进程只通过目录枚举与 stat 比对构建身份、精确 Provider 入口及源/镜像文件的 size、mtime、ctime，任一差异才回到内容摘要校验与修复。稳定 profile 摘要隔离显式 userData 场景；长耗时复制持续刷新 Provider 启动租约，lease 失效必须在 spawn 前阻断旧 owner。启动锁、镜像发布锁及其 mutation guard 都记录 PID 与进程内唯一 epoch lease；回收方通过本机命名管道或私有 Unix socket 验证精确 lease，不能把复用同一 PID 的后继进程当作原 owner。guard 先在唯一候选目录完整写入 owner 再原子发布，回收 fence 只能退役到不复用的唯一目录后删除；精确活跃的 guard 不得因等待超时被强抢，启动/发布租约则以 stale heartbeat 作为挂起 owner 的最终恢复边界。staging 未写最终 marker 前不具备发布资格；并发进程通过发布租约收敛到同一不可变镜像，残缺或关键运行文件失效的目标只能在逐镜像发布锁内隔离。warm 校验、发布与 prune 复用同一把逐镜像锁，并在返回或删除前再次验证 lease。迁移 host 被系统策略或 AV 拒绝 spawn 时只允许一次安装目录 host 回退。解析出新 Provider 的启动目标后，Platform 保留当前构建镜像及 metadata 中经 generation heartbeat 判定为 alive/starting 的旧镜像；dead generation 不再因 PID 复用永久 pin，unknown 则跳过整轮清理。其余镜像以首次不再属于 current/pinned 集合时写入的持久化 retirement marker 开始计时，至少保留 24 小时后才 best-effort 删除；镜像在 warm 校验、发布或 pin 时清除旧 marker，使下次退役重新计时。逐镜像锁关闭到 provisional metadata 发布之间仍由新一轮完整宽限保护，metadata 撤销到旧 Provider 完全退出之间也不会使仍在执行的 host、ASAR 或 native addon 被立即移除。物化或清理失败不能杀死既有 Provider。普通新安装默认采用 current-user，legacy machine-wide 安装继续保留原作用域；真实卸载终止 Provider 进程树并清理执行卸载账户的 LocalAppData，但不跨 profile 删除其他用户的惰性缓存。该机制保证兼容协议更新时旧 Provider 的映像、ASAR、native addon 与 ConPTY runtime 不再依赖旧安装目录，但不替代跨破坏性协议版本的并行 Provider 路由。

应用数据根由 Platform 在单实例锁和 Run 装配前确定：发布包使用正式 profile，未打包开发运行按 CleanCode 源码 worktree 使用稳定独立 profile，显式 `--user-data-dir` 继续拥有最高优先级。`userData`、Chromium `sessionData`、Provider metadata 和恢复资料必须落在同一 profile 边界内；不同开发 worktree 不得复用 Provider 或 checkpoint。Windows runtime image 是可重建的本机执行材料，单独位于 LocalAppData，不进入 roaming profile；E2E 只有在显式测试标记存在、场景状态目录位于系统临时目录且镜像目录严格位于该场景目录内时才能覆盖位置，并在 Provider 停止后统一清理。

Run 基础设施在应用数据目录保存 schema v2 checkpoint record 与 schema v1 追加式输出记录。record 同时保存不可变源主题；checkpoint 包含有界普通/alternate 屏幕、可读 normal buffer 历史、cwd、行列、标题、模式和最后 sequence；输出记录只重放 checkpoint 之后的连续 sequence。读取旧 schema v1 record 时迁移为确定性的深色源主题，解决旧数据缺少该事实且无法从序列化屏幕可靠反推的问题。每个 session 的 Provider 持久化协调器拥有待追加输出、批处理调度器和串行持久化尾链；同一事件循环轮次内的连续输出执行 group commit，一批只做一次容量检查、文件打开、追加、同步和关闭，JSONL record 与 sequence 顺序保持不变。checkpoint、进程退出、controller detach、Provider close 和 session retire 是强制屏障：checkpoint 先处理已接收批次再捕获权威模型，retire 在删除资料前等待在途写入。checkpoint 写入继续使用同目录临时文件、同步、原子重命名和目录同步；单 checkpoint、输出日志、冷历史数量、保留期和全局字节数都有上限，容量清理只淘汰冷历史，不为腾出空间终止 live 会话。损坏或未知版本按 session 隔离。首次启用保留无法完成 checkpoint 时动作失败并回滚；后续持久化失效时 Provider 撤销保留并通知应用，应用退出时安全终止该会话。

应用重开优先对已认证 Provider 中仍存活的精确会话执行 warm attach，先取得 Provider 权威 snapshot，再接续实时 sequence。Provider 已丢失时，新 Provider 只从 checkpoint 和连续输出记录恢复 normal-buffer 历史、cwd、尺寸与模式，产生无进程声明的只读 `historical` 会话；alternate screen 不覆盖可读历史。损坏资料不会阻断其他会话，新建 shell 必须使用新的 `sessionId + runId + generation`。

工作流执行可能复用普通终端会话作为输出承载，但工作流命令使用独立命令 PTY，并由 `TerminalSessionWorkflowRuntimeAdapter` 协调；不得把普通会话的人工输入当作工作流成功信号，也不得把 Provider 中的单个 PTY 恢复投影为已恢复的 `WorkflowRun`。

## 实现入口

| 层级           | 入口                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Domain         | [`TerminalSession.ts`](../../../src/contexts/run/domain/aggregates/TerminalSession.ts)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Application    | [`TerminalSessionService.ts`](../../../src/contexts/run/application/use-cases/TerminalSessionService.ts)、[`TerminalCapabilityEnvironment.ts`](../../../src/contexts/run/application/services/TerminalCapabilityEnvironment.ts)、[`TerminalPalette.generated.ts`](../../../src/contexts/run/application/dto/TerminalPalette.generated.ts)、[`ForegroundJob.ts`](../../../src/contexts/run/domain/aggregates/ForegroundJob.ts)、[`LaunchTerminalCommandUseCase.ts`](../../../src/contexts/run/application/use-cases/LaunchTerminalCommandUseCase.ts)、[`OpenTerminalLinkUseCase.ts`](../../../src/contexts/run/application/use-cases/OpenTerminalLinkUseCase.ts)、[`RunLifecycleService.ts`](../../../src/contexts/run/application/use-cases/RunLifecycleService.ts)、[`TerminalProcessPort.ts`](../../../src/contexts/run/application/ports/TerminalProcessPort.ts)、[`TerminalModelPort.ts`](../../../src/contexts/run/application/ports/TerminalModelPort.ts)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Infrastructure | [`NodePtyTerminalProcessAdapter.ts`](../../../src/contexts/run/infrastructure/pty/NodePtyTerminalProcessAdapter.ts)、[`WindowsConptyWarmup.ts`](../../../src/contexts/run/infrastructure/pty/WindowsConptyWarmup.ts)、[`TerminalProcessEnvironment.ts`](../../../src/contexts/run/infrastructure/pty/TerminalProcessEnvironment.ts)、[`HeadlessTerminalModelAdapter.ts`](../../../src/contexts/run/infrastructure/terminal-model/HeadlessTerminalModelAdapter.ts)、[`TerminalProviderServer.ts`](../../../src/contexts/run/infrastructure/provider/TerminalProviderServer.ts)、[`TerminalProviderControllerLifecycle.ts`](../../../src/contexts/run/infrastructure/provider/TerminalProviderControllerLifecycle.ts)、[`TerminalProviderShutdownCoordinator.ts`](../../../src/contexts/run/infrastructure/provider/TerminalProviderShutdownCoordinator.ts)、[`TerminalProviderSessionPersistence.ts`](../../../src/contexts/run/infrastructure/provider/TerminalProviderSessionPersistence.ts)、[`PersistentTerminalProviderClient.ts`](../../../src/contexts/run/infrastructure/provider/PersistentTerminalProviderClient.ts)、[`TerminalProviderProcessLauncher.ts`](../../../src/contexts/run/infrastructure/provider/TerminalProviderProcessLauncher.ts)、[`TerminalProviderLaunchReadiness.ts`](../../../src/contexts/run/infrastructure/provider/TerminalProviderLaunchReadiness.ts)、[`TerminalProviderMetadataStore.ts`](../../../src/contexts/run/infrastructure/provider/TerminalProviderMetadataStore.ts)、[`TerminalProviderHeartbeat.ts`](../../../src/contexts/run/infrastructure/provider/TerminalProviderHeartbeat.ts)、[`FileSystemMutationLock.ts`](../../../src/contexts/run/infrastructure/provider/FileSystemMutationLock.ts)、[`ProcessEpochLiveness.ts`](../../../src/contexts/run/infrastructure/provider/ProcessEpochLiveness.ts)、[`FileTerminalRecoveryStore.ts`](../../../src/contexts/run/infrastructure/persistence/FileTerminalRecoveryStore.ts)、[`NodeTerminalLinkFileSystemAdapter.ts`](../../../src/contexts/run/infrastructure/filesystem/NodeTerminalLinkFileSystemAdapter.ts) |
| Platform       | [`terminal-runtime-provider.ts`](../../../src/platform/electron-main/terminal-runtime-provider.ts)、[`terminalProviderRuntimeImage.ts`](../../../src/platform/electron-main/terminalProviderRuntimeImage.ts)、[`TerminalProviderRuntimeImagePublishLock.ts`](../../../src/platform/electron-main/TerminalProviderRuntimeImagePublishLock.ts)、[`runtimeDataDirectoryPolicy.ts`](../../../src/platform/electron-main/runtimeDataDirectoryPolicy.ts)、[`runtimeDataDirectoryBootstrap.ts`](../../../src/platform/electron-main/runtimeDataDirectoryBootstrap.ts)、[`applicationRuntimeShutdown.ts`](../../../src/platform/electron-main/applicationRuntimeShutdown.ts)、[`terminalIpcHandlers.ts`](../../../src/platform/electron-main/terminalIpcHandlers.ts)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

Run Presentation 通过 [`terminalRuntimePreference.ts`](../../../src/contexts/run/presentation/view-models/terminalRuntimePreference.ts) 持有滚屏预算偏好的本地投影，并由 [`TerminalScrollbackSettingsSection.tsx`](../../../src/contexts/run/presentation/components/TerminalScrollbackSettingsSection.tsx) 展示受限 preset；应用设置页只负责组合该 section，不重新定义预算事实。

普通终端的 xterm mount、attach/detach、恢复、搜索、输入、粘贴确认、链接打开、resize 和固定源主题投影由 [`TerminalRuntimeViewport.tsx`](../../../src/contexts/run/presentation/components/TerminalRuntimeViewport.tsx) 统一拥有。该组件只接收 Run 的 `TerminalViewState`、运行回调和用于可访问名称的 `blockName`，不接收 BlockGraph snapshot。App Shell 的 `TerminalViewport` adapter 负责从 BlockGraph 终端定义投影显示名，并把 Run 的纯输入回调重新绑定到当前 block；`TerminalNode` 继续负责节点、图动作与 Run runtime 的跨上下文组合。

终端节点头部的启动、停止当前命令、退出保留和重开动作由 [`TerminalRuntimeHeader.tsx`](../../../src/contexts/run/presentation/components/TerminalRuntimeHeader.tsx) 中的窄 Run 组件拥有；对应专属状态样式位于 [`terminal-runtime-header.css`](../../../src/contexts/run/presentation/styles/terminal-runtime-header.css)。组件只接收已投影的终端显示名、运行状态、启动可用性和回调，不读取 BlockGraph snapshot。动作轨基础布局以及编辑、删除和组合候选仍由 App Shell/BlockGraph 组合层拥有。

## 验证矩阵

滚屏设置的 owner-level Presentation 验证由 [`run.terminal-scrollback-settings.presentation.spec.tsx`](../../../tests/unit/contexts/run/run.terminal-scrollback-settings.presentation.spec.tsx) 提供；完整设置页导航与组合仍由根级 Application Settings 测试覆盖。普通终端 viewport 的 attach identity、恢复、输入、搜索、粘贴、链接、resize 和主题固定由 `tests/unit/contexts/run/run.terminal-runtime-viewport.*.presentation.spec.tsx` 覆盖；BlockGraph 参数收窄和 block-scoped 回调重绑由根级 `terminal-viewport-adapter.spec.tsx` 覆盖。节点头部 Run 动作的顺序、状态、图标和回调由 [`run.terminal-runtime-header.presentation.spec.tsx`](../../../tests/unit/contexts/run/run.terminal-runtime-header.presentation.spec.tsx) 覆盖，完整七动作组合与 BlockGraph 回调继续由根级 `terminal-tooltips.spec.tsx` 覆盖。

| 层级            | 证明内容                                                                                                                                                                                                                                                                                                                                               | 主要测试                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Unit / Domain   | 状态迁移、退出策略、恢复资格与仅运行中可输入                                                                                                                                                                                                                                                                                                           | [`run.terminal-session.spec.ts`](../../../tests/unit/contexts/run/run.terminal-session.spec.ts)、[`run.terminal-runtime-recovery.spec.ts`](../../../tests/unit/contexts/run/run.terminal-runtime-recovery.spec.ts)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Unit / Service  | typed owner、ForegroundJob、精确作用域、替换、迟到事件、sequence、视图、Ctrl+C、环境 preparation、lifecycle observer 与单次 Provider shutdown handoff                                                                                                                                                                                                  | [`run.terminal-owner.spec.ts`](../../../tests/unit/contexts/run/run.terminal-owner.spec.ts)、[`run.foreground-job.spec.ts`](../../../tests/unit/contexts/run/run.foreground-job.spec.ts)、[`run.terminal-session-service.spec.ts`](../../../tests/unit/contexts/run/run.terminal-session-service.spec.ts)、[`run.terminal-session-lifecycle-observer.spec.ts`](../../../tests/unit/contexts/run/run.terminal-session-lifecycle-observer.spec.ts)、[`run.terminal-session-model-lifecycle.spec.ts`](../../../tests/unit/contexts/run/run.terminal-session-model-lifecycle.spec.ts)、[`run.run-lifecycle-service.spec.ts`](../../../tests/unit/contexts/run/run.run-lifecycle-service.spec.ts)、[`run.terminal-application-shutdown.spec.ts`](../../../tests/unit/contexts/run/run.terminal-application-shutdown.spec.ts)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Unit / Provider | 启动/发布租约、mutation guard、进程 epoch、generation heartbeat 的 ABA/PID 复用边界，以及并发 shutdown、终止/保留分类、checkpoint 失败降级与证据保留                                                                                                                                                                                                   | [`run.file-provider-launch-lock-lease.spec.ts`](../../../tests/unit/contexts/run/run.file-provider-launch-lock-lease.spec.ts)、[`run.file-system-mutation-lock.spec.ts`](../../../tests/unit/contexts/run/run.file-system-mutation-lock.spec.ts)、[`run.process-epoch-liveness.spec.ts`](../../../tests/unit/contexts/run/run.process-epoch-liveness.spec.ts)、[`run.terminal-provider-heartbeat.spec.ts`](../../../tests/unit/contexts/run/run.terminal-provider-heartbeat.spec.ts)、[`run.terminal-provider-launch.spec.ts`](../../../tests/unit/contexts/run/run.terminal-provider-launch.spec.ts)、[`run.terminal-provider-shutdown-coordinator.spec.ts`](../../../tests/unit/contexts/run/run.terminal-provider-shutdown-coordinator.spec.ts)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Unit / Host     | 过滤 Provider 私有 Electron Node 模式、PowerShell UTF-8/Profile 参数、自动 shell 与 Store Alias 解析、同步 spawn 回退、一次性 ConPTY 预热、首屏前关闭与重复关闭幂等清理、保留显式环境与终端能力、source-theme 明暗语义、生成色板被 xterm 与隐藏模型共同消费                                                                                            | [`run.powershell-utf8-bootstrap.spec.ts`](../../../tests/unit/contexts/run/run.powershell-utf8-bootstrap.spec.ts)、[`run.terminal-shell-command.spec.ts`](../../../tests/unit/contexts/run/run.terminal-shell-command.spec.ts)、[`run.terminal-shell-executable-resolver.spec.ts`](../../../tests/unit/contexts/run/run.terminal-shell-executable-resolver.spec.ts)、[`run.terminal-shell-executable-resolver-alias.spec.ts`](../../../tests/unit/contexts/run/run.terminal-shell-executable-resolver-alias.spec.ts)、[`run.windows-powershell-spawn-fallback.spec.ts`](../../../tests/unit/contexts/run/run.windows-powershell-spawn-fallback.spec.ts)、[`run.foreground-job-shell-control.spec.ts`](../../../tests/unit/contexts/run/run.foreground-job-shell-control.spec.ts)、[`run.windows-conpty-warmup.spec.ts`](../../../tests/unit/contexts/run/run.windows-conpty-warmup.spec.ts)、[`run.node-pty-windows-exit.spec.ts`](../../../tests/unit/contexts/run/run.node-pty-windows-exit.spec.ts)、[`run.terminal-process-environment.spec.ts`](../../../tests/unit/contexts/run/run.terminal-process-environment.spec.ts)、[`run.terminal-capability-environment.spec.ts`](../../../tests/unit/contexts/run/run.terminal-capability-environment.spec.ts)、[`run.terminal-source-palette.spec.ts`](../../../tests/unit/contexts/run/run.terminal-source-palette.spec.ts)、[`run.terminal-theme-palette.spec.ts`](../../../tests/unit/contexts/run/run.terminal-theme-palette.spec.ts)、[`check-theme.spec.ts`](../../../tests/unit/support/check-theme.spec.ts)                                                                                                                             |
| Integration     | 真实 node-pty、Windows PowerShell/Agent UTF-8、损坏 `pwsh.exe` 到 inbox PowerShell 的真实同步回退、ConPTY 同步连接失败回收、首屏前重复关闭、预热自然退出与并发关闭、环境过滤、Provider 启动租约、metadata/heartbeat 条件交接、并发连接、v7 协议边界、批量 shutdown、checkpoint/cold restore、headless 模型、alternate buffer、背压、路径边界和进程清理 | [`run.pty-terminal.spec.ts`](../../../tests/integration/contexts/run/run.pty-terminal.spec.ts)、[`run.windows-agent-pty.spec.ts`](../../../tests/integration/contexts/run/run.windows-agent-pty.spec.ts)、[`run.windows-powershell-spawn-fallback.spec.ts`](../../../tests/integration/contexts/run/run.windows-powershell-spawn-fallback.spec.ts)、[`run.node-pty-windows-conpty-connect-failure.spec.ts`](../../../tests/integration/contexts/run/run.node-pty-windows-conpty-connect-failure.spec.ts)、[`run.node-pty-windows-conpty-close-race.spec.ts`](../../../tests/integration/contexts/run/run.node-pty-windows-conpty-close-race.spec.ts)、[`run.terminal-provider-launch-lock.spec.ts`](../../../tests/integration/contexts/run/run.terminal-provider-launch-lock.spec.ts)、[`run.terminal-provider-metadata.spec.ts`](../../../tests/integration/contexts/run/run.terminal-provider-metadata.spec.ts)、[`run.terminal-provider-client-lifecycle.spec.ts`](../../../tests/integration/contexts/run/run.terminal-provider-client-lifecycle.spec.ts)、[`run.terminal-provider-shutdown.spec.ts`](../../../tests/integration/contexts/run/run.terminal-provider-shutdown.spec.ts)、[`run.terminal-provider-server.spec.ts`](../../../tests/integration/contexts/run/run.terminal-provider-server.spec.ts)、[`run.file-terminal-recovery-store.spec.ts`](../../../tests/integration/contexts/run/run.file-terminal-recovery-store.spec.ts)、[`run.headless-terminal-model.spec.ts`](../../../tests/integration/contexts/run/run.headless-terminal-model.spec.ts)、[`run.terminal-link-filesystem.spec.ts`](../../../tests/integration/contexts/run/run.terminal-link-filesystem.spec.ts) |
| Platform        | 开发 profile 稳定隔离、Electron 数据路径接线、Windows Provider runtime image 闭包、warm fingerprint、逐镜像发布/prune 与退役宽限、更新/卸载边界，以及分阶段 shutdown、超时、single-flight 与结构化诊断                                                                                                                                                 | [`runtime-data-directory-policy.spec.ts`](../../../tests/unit/platform/runtime-data-directory-policy.spec.ts)、[`runtime-data-directory-bootstrap.spec.ts`](../../../tests/unit/platform/runtime-data-directory-bootstrap.spec.ts)、[`terminal-provider-runtime-environment.spec.ts`](../../../tests/unit/platform/terminal-provider-runtime-environment.spec.ts)、[`terminal-provider-runtime-image.spec.ts`](../../../tests/unit/platform/terminal-provider-runtime-image.spec.ts)、[`terminal-provider-runtime-image-warm-start.spec.ts`](../../../tests/unit/platform/terminal-provider-runtime-image-warm-start.spec.ts)、[`terminal-provider-runtime-image-publish-lock-io.spec.ts`](../../../tests/unit/platform/terminal-provider-runtime-image-publish-lock-io.spec.ts)、[`run.terminal-provider-launch.spec.ts`](../../../tests/unit/contexts/run/run.terminal-provider-launch.spec.ts)、[`windows-terminal-provider-uninstall.spec.ts`](../../../tests/unit/support/windows-terminal-provider-uninstall.spec.ts)、[`application-runtime-shutdown.spec.ts`](../../../tests/unit/platform/application-runtime-shutdown.spec.ts)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Contract        | snapshot、scrollback、restore marker、视图身份、链接命令、定向输出和 renderer 销毁清理                                                                                                                                                                                                                                                                 | [`run.terminal-view-ipc.spec.ts`](../../../tests/contract/contexts/run/run.terminal-view-ipc.spec.ts)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Unit / UI 协作  | 工作区切换、Agent 输入合并、owner 输出隔离与无匹配状态引用保持                                                                                                                                                                                                                                                                                         | [`run.terminal-session-workspace-migration.spec.ts`](../../../tests/unit/contexts/run/run.terminal-session-workspace-migration.spec.ts)、[`agent-terminal-view.spec.tsx`](../../../tests/unit/presentation/agent-terminal-view.spec.tsx)、[`run.terminal-session-output-buffer.spec.ts`](../../../tests/unit/contexts/run/run.terminal-session-output-buffer.spec.ts)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| E2E             | 输入/视图重建、应用与 renderer/main/Provider 故障、中断启动租约、并发终端重启、warm/history 区分、Windows 打包态 runtime image、旧安装目录退役与替换包 warm attach、永久关闭、项目移除和工作流不恢复                                                                                                                                                   | [`run-terminal-sessions.e2e.spec.ts`](../../../tests/e2e/run-terminal-sessions.e2e.spec.ts)、[`git-branch-workspaces.e2e.spec.ts`](../../../tests/e2e/git-branch-workspaces.e2e.spec.ts)、[`terminal-daily-interactions.e2e.spec.ts`](../../../tests/e2e/terminal-daily-interactions.e2e.spec.ts)、[`terminal-runtime-recovery.e2e.spec.ts`](../../../tests/e2e/terminal-runtime-recovery.e2e.spec.ts)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

## 维护规则

改变会话槽位、状态机、输入/中断语义、模型和视图交接、输出 sequence、终端能力环境、canonical palette、工作目录查询或持久化策略时，必须同步聚合、服务、端口、适配器、测试和本文。工作流专属状态不得并入本文的普通会话状态机。
